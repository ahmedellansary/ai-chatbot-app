[PATCH] In the preview function, replace the hardcoded src='index.html' with: const activeFile = window.currentActiveFile || 'index.html'; previewIframe.src = activeFile; Also ensure that whenever the user switches files in the editor, the variable window.currentActiveFile is updated to the selected filename (e.g., inside the file-switch handler, add: window.currentActiveFile = selectedFile;).